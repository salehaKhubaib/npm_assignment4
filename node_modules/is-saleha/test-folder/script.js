// app.js

const reverseString = require('is-saleha'); // Assuming both files are in the same directory

let originalString = "Saleha Khubaib";
let reversed = reverseString(originalString);
console.log(reversed); // Outputs: "!dlroW ,olleH"
